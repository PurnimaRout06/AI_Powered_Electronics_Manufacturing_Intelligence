# Base URL for the Node.js/Express backend once it's available.
VITE_API_BASE_URL=http://localhost:4000/api

# While true, all services/*.js resolve data from src/data/mockData.js
# instead of calling the API. Set to false once the Express endpoints
# listed in each service file are live.
VITE_USE_MOCKS=true
